{
  "name": "json-server",
  "version": "0.0.1",
  "scripts": {
    "build": "node build",
    "start": "node index",
    "test": "node test/app"
  },
  "dependencies": {
    "clone": "^2.1.2",
    "json-server": "^0.17.0"
  }
}
